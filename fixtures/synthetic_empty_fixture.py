"""
Empty synthetic Python package for unit tests.

This file exists only so the ``fixtures/`` directory has a tracked
placeholder. Real fixtures will be added in later milestones and
must be clearly labelled as synthetic (the ``synthetic_`` prefix
in the filename is the convention).
"""

NAME = "synthetic-empty-fixture"
VERSION = "0.0.0"
